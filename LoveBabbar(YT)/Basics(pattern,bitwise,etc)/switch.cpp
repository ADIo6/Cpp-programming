#include<iostream>
using namespace std;

int main()
{
    int num;
    cin>>num;

    while(1)
    {
        switch (num)
        {
        case 1: cout<<"no. 1"<<endl;
            /* code */

        exit(0);
        default:cout<<"its default"<<endl;


            exit(0);
        }
        
    }
}